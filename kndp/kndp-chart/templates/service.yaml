apiVersion: v1
kind: Service
metadata:
  name: {{ include "kndp.fullname" . }}
  labels:
    {{- include "kndp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kndp.selectorLabels" . | nindent 4 }}
