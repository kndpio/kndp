apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kndp.fullname" . }}-test-connection"
  labels:
    {{- include "kndp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kndp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
