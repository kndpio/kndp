apiVersion: v1
kind: Service
metadata:
  name: {{ include "development-view.fullname" . }}
  labels:
    {{- include "development-view.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "development-view.selectorLabels" . | nindent 4 }}
