{
  "root": "packages/kndp/backstage",
  "version": 2,
  "role": "backstage",
  "sourceRoot": "packages/kndp/backstage/src",
  "projectType": "application",
  "tags": ["scope:kndp"],
  "targets": {
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["concurrently \"nx run app:start\" \"nx run backend:start\""]
      }
    }
        }
      }
    
  